using System;

class MainClass {
  public static void Main (string[] args) {
    double PI = Math.PI;
    float pi1 = (float)PI;
    Console.WriteLine ("Voici la constante Pi plus précise :  " + PI);
    Console.WriteLine ("Voici la constante Pi moins précise : " + pi1);
  }
}
